/**
 * Default view names for the scheduler, used when no available views are specified.
 */
export const DEFAULT_VIEW_NAMES = ["day", "week", "month", "year", "event"];

// Add more constants here as needed, for example:
export const DEFAULT_DATE_FORMAT = "YYYY-MM-DD";
export const DEFAULT_TIME_FORMAT = "HH:mm:ss";
export const SUPPORTED_LANGUAGES = ["en", "es", "fr", "de", "pt"];